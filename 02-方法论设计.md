# AI Product Court 方法论设计：Runtime 流程与 Agent 角色体系

## 核心洞察：寻找稀缺的"反向淘汰"机制

如果 AI 已经能提出 100 个产品创意，产品经理真正缺的还是创意吗？我们认为不是——真正稀缺的，是一套能快速证明"哪些创意不值得做"的机制。

即便在流程完善的头部消费电子企业，传统产品定义模式依然存在三个由"人肉决策机制"带来的物理上限：

- **洞察规模受限**：用户调研依赖小样本访谈，难以系统覆盖海量长尾细分场景，冷门但高频的痛点易被忽略。
- **评审依赖个体经验**：历史踩坑经验分散在个人脑中，极易随人员流动而流失，新人团队难以复用。
- **并行试错成本高企**：多方案并行验证需要走完原型开发、小批量测试的全周期，时间与资金成本极高。

因此，我们没有设计一个"更会想创意"的 AI，而是设计了一套"更会排除错误"的 AI Product Court（产品仿真评审法庭）。真正的竞争优势，不是谁第一次想到完美的答案，而是谁能以最低的成本、最快的速度系统性排除错误答案。

---

## 系统 Runtime：7 步全链路

AI Product Court 不是"开三个 ChatGPT 窗口然后人工比较"。每个 Agent 被赋予独立且可能互斥的优化目标，在统一 Runtime 中按固定流程协作：

```
产品想法输入
      ↓
Idea Normalizer（统一需求描述与评审字段）
      ↓
Evidence Retrieval（检索历史决策 + 用户证据 + 竞品数据 + 企业规则）
      ↓
Agent Debate Round（各 Agent 独立判断，互不共享偏好）
      ↓
Counterfactual Simulation（对分歧焦点并行推演多个未来路径）
      ↓
Conflict Resolver（提炼对立焦点，不输出 yes/no 投票）
      ↓
Human Arbitration（仅在高分歧无法收敛时介入）
      ↓
Decision Memory（结构化写入 Product Brain，反哺下一轮检索）
```

---

## Agent 优化目标体系

每个 Agent 被赋予独立且可能互斥的优化目标，这正是"多智能体系统"和"开三个 ChatGPT 窗口"的本质区别：

| Agent | 优化目标 | 它追问什么 |
|-------|----------|-----------|
| 超级智囊 | 最大化机会价值 | "这个方向为什么值得做？证据在哪？" |
| 成本专家 | 最小化商业风险 | "BOM 是否在预算红线内？" |
| 安全合规专家 | 最大化风险规避 | "是否触碰 GDPR/海外监管红线？" |
| 市场专家 | 最大化差异化窗口 | "竞品是否已覆盖？窗口期还有多久？" |
| 用户替身（3 画像） | 最大化真实需求匹配 | "我会不会掏钱买？我最担心什么？" |

各 Agent 连接的不是通用搜索引擎，而是特定的**组织上下文**——历史决策记录、企业规则红线、用户真实反馈——在生成判断前先检索相关证据。这区别于让 AI 获得更多知识（RAG），而是让 AI 获得**组织上下文**（Context Engineering）。

---

## 三大核心差异化机制

### Counterfactual Simulation（反事实并行推演）

传统产品评审只能评估"这个方案好不好"，AI Product Court 在此基础上并行推演多个未来路径——做了会怎样（销量/利润/品牌影响）、不做会怎样（竞品抢先/市场教育缺失）、在不同时间窗口做会怎样（成本下降后/用户习惯养成后）。这解决的不再是"这个功能好不好"，而是"现在做是不是时候"——消费电子行业最致命的决策失误往往不是选错方向，而是选错节奏。

### 为什么这才是 AI 原生

传统产品流程的核心限制，不是专家不够好，而是人类只能在有限时间内评估有限数量的方案——一个想法 → 召集 5 个专家 → 开 2 小时会 → 结束。AI Product Court 的本质不是替代专家，而是利用 AI 的低成本并行推演能力，在产品进入真实开发前模拟更多可能未来。**核心差异不是真人 vs AI，而是单线程决策 vs 多未来并行模拟。**

### Product Brain 决策记忆层

各 Agent 的交叉质询是手段，不是目的。真正不可替代的，是评审之后沉淀下来的东西。Product Brain 不是知识库——它把每一次产品决策转化为可检索的决策边界模型，存储的不是"掌静脉被否决了"这个结论，而是"为什么当时这么判断、什么情况下这个判断应该被推翻"。

**四层架构**：

| 层 | 功能 | 核心问题 |
|----|------|----------|
| Decision Records | 决策案例的结构化存储 | "发生了什么" |
| Decision Patterns | Decision Mining Agent 自动挖掘决策规律 | "这类方案的历史存活率" |
| Boundary Conditions | 反对条件 + 重新评估条件 | "什么时候该推翻" |
| Challenger Memory | 记录"当时为什么可能错" | "这次会不会也错了" |

四层结构共同回答一个核心问题：**新提案属于哪类历史决策模式，当时的边界条件现在是否仍然成立。**

---

## Agent 角色详解

### ① 超级智囊 Agent（提案者）

**优化目标**：最大化机会价值

**工作方式**：交叉检索用户证据库（全网差评）、竞品知识库与产品案例库，同时前置注入 Decision Graph 中的历史决策记录作为避坑护栏，走通透明数据链路——用户证据 → 竞品交叉验证 → 机会点确认 → 产品概念生成。

### ② 行业专家 Agent 矩阵（质检员）

**优化目标**：6 种互相独立且可能冲突的优化目标——成本、安全合规、市场、品牌、供应链、客服。

**工作方式**：每个目标对应一个专业评审视角，依据企业规则库进行强制校验。当专家间出现矛盾判断时，系统自动提炼真正的决策冲突点。

**Anti-Bias Agent**：当 Evidence Retrieval 检索到历史否决记录时，检查该决策的成立条件是否已发生变化（技术成本是否下降、法规是否更新、用户偏好是否迁移）。若条件已变，则标记"建议重新评估"。

### ③ 用户替身 Agent（用户代言人）

**优化目标**：最大化真实需求匹配

**工作方式**：灌注海外真实评论数据，模拟"独居青年、多代同住、短租房东"三类画像，生成 Top 10 Killer Questions 质询方案。把原本要靠产品上市后才能遭遇的市场检验，提前在立项阶段仿真演练。

### ④ Product Brain（决策记忆层）

**工作方式**：每一轮评审的完整决策链条结构化写入飞书多维表格。Decision Mining Agent 定期扫描全量决策记录，提炼反复出现的决策规律并反向注入 Agent 的生成策略——Product Brain 不是被动存档，而是主动改变 AI 的生成行为。
