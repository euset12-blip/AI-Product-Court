# AI Product Court 飞书原生落地全执行手册

核心原则：**零前端开发、全原生能力、流程可复用、数据可沉淀**，完全基于飞书文档、多维表格、自动化流程、智能伙伴、知识库五大核心能力搭建，对齐安克现有办公工具栈，实现即插即用的产品评审流水线。

---

## 一、前置一次性搭建：飞书内基础底座配置

该阶段为一次性配置工作，无需代码开发，仅靠飞书原生功能即可完成，1-2天即可搭完最小可用版本。

### 1. 统一提案入口：Idea Normalizer 标准化层

解决"创意描述五花八门、评审标准不统一"的问题，用固定字段强制所有提案对齐同一评审框架。

- **落地工具**：飞书表单 + 飞书多维表格「提案池」
- **具体操作**：
  1. 制作标准化提案表单，固定必填字段：提案名称、所属品类、核心用户痛点、核心功能描述、目标用户画像、预估BOM影响、预期商业价值、竞品对标情况。
  2. 表单提交后自动同步至「提案池多维表格」，系统自动生成唯一提案ID、自动打标签（品类、功能类型、成本等级），完成需求的标准化归一。
  3. 配置飞书自动化：新提案入库后，**自动基于飞书文档模板生成对应评审文档**，文档自带固定评审框架（证据区、各专家评审区、推演区、裁决区），文档链接自动回写至多维表格。

### 2. 四大证据库：Evidence Retrieval 的数据源

所有Agent的判断依据全部来自飞书内的结构化数据，而非通用网络信息，保证组织上下文的准确性。

- **落地工具**：飞书知识库 + 4张独立多维表格
- 具体分为4类证据源，全部在飞书内维护：
  1. **历史决策库**：即Product Brain的底层数据，存储所有过往评审的完整决策链（详见第三部分）。
  2. **用户证据库**：导入全网差评、客服工单、用户访谈纪要，按「品类-场景-痛点」打标签，支持语义检索。
  3. **竞品知识库**：录入竞品功能参数、定价、卖点、市场反馈，按品牌和产品线结构化存储。
  4. **企业规则库**：固化BOM红线、GDPR合规标准、品牌调性要求、供应链约束等硬性规则，作为专家Agent的强制校验依据。

### 3. Agent 角色矩阵：飞书智能伙伴定制

每个评审角色对应一个独立的飞书智能伙伴，预设专属系统提示词与知识库访问权限，确保各Agent目标独立、视角互斥。

- **落地工具**：飞书智能伙伴（自定义角色）
- **配置规则**：
  1. 为超级智囊、成本专家、合规专家、市场专家、3类用户替身、Anti-Bias Agent、Decision Mining Agent分别创建独立智能伙伴。
  2. 每个伙伴仅授权访问对应证据库：例如成本专家仅可读取BOM规则库与供应链库，合规专家仅可读取全球合规规则库，避免信息交叉干扰。
  3. 预设固定输出格式：每个Agent必须按「结论+核心依据+风险评分（0-1）+补充建议」四部分输出，保证评审结果可结构化提取。

### 4. 自动化流转规则：飞书流程自动化配置

用飞书自动化替代人工流转，全流程无需PM手动拉人、催评审，系统自动按节点推进。

- **落地工具**：飞书自动化（原飞书捷径）
- 核心触发规则：
  - 新提案入库 → 自动生成评审文档 → 自动触发证据检索
  - 证据检索完成 → 自动@所有Agent进入评审环节
  - 所有Agent评审完成 → 自动触发冲突提炼与反事实推演
  - 人工仲裁完成 → 自动结构化归档至Product Brain → 自动通知提案人

---

## 二、端到端执行流程：Runtime 全链路飞书操作

完整对应AI Product Court的7步Runtime，每一步都有明确的飞书操作载体、执行主体与产出物。

### 1. 提案录入与标准化（Idea Normalizer）

- **操作主体**：提案人（PM/运营/研发）
- **飞书操作**：填写标准化提案表单，提交后自动进入「提案池多维表格」，系统自动生成带模板的评审文档。
- **产出**：标准化提案条目 + 结构化评审文档，所有后续评审全部在该文档内完成。

### 2. 自动证据检索（Evidence Retrieval）

- **操作主体**：飞书自动化 + 检索Agent
- **飞书操作**：
  1. 系统根据提案的品类、标签，自动在四大证据库中做语义检索，匹配历史决策、用户痛点、竞品数据、企业规则。
  2. 检索结果自动插入评审文档的「证据参考」章节，同时标注来源（如"来自BIO-001号历史决策""来自北美用户差评库"）。
  3. Anti-Bias Agent同步校验：若检索到同类历史否决记录，自动核查当前边界条件是否变化，标注「建议沿用结论」或「建议重新评估」。
- **产出**：带引用来源的证据合集，所有Agent基于同一套证据做判断，避免信息差。

### 3. 多Agent独立评审（Agent Debate Round）

- **操作主体**：多个独立Agent（超级智囊+行业专家+用户替身）
- **飞书操作**：
  1. 系统自动@对应智能伙伴到评审文档的指定分区，每个Agent仅能在自己的专属评审区输出意见，无法读取其他Agent的内容，保证视角独立。
  2. 各Agent基于证据库内容，按固定格式输出评审结论与风险评分，全程无人工干预。
  3. 用户替身Agent额外输出该画像下的Top 5质疑问题（Killer Questions），直接写入文档的「用户挑战」章节。
- **产出**：多视角独立评审意见，带量化风险评分，无互相干扰的主观站队。

### 4. 反事实并行推演（Counterfactual Simulation）

- **操作主体**：推演Agent
- **飞书操作**：
  1. 系统识别评审中的核心分歧点，自动触发3条路径并行推演：立即落地、延迟落地、完全不做。
  2. 推演维度固定为：成本影响、利润表现、竞品差距、用户价值、合规风险5个维度，结果自动生成对比表写入评审文档。
- **产出**：多场景决策推演表，回答"什么时候做、做了会怎样、不做会怎样"的节奏问题。

### 5. 冲突焦点自动提炼（Conflict Resolver）

- **操作主体**：冲突解析Agent
- **飞书操作**：
  1. 扫描所有Agent的评审意见与推演结果，自动提炼「共识点」与「核心冲突点」，不输出yes/no的投票结论。
  2. 对冲突做分级：低分歧（风险分差<0.2）自动收敛，高分歧（风险分差>0.5）标记为「需人工仲裁」。
  3. 结果自动同步至「提案池多维表格」，同时@负责PM在飞书内收到通知。
- **产出**：结构化冲突摘要，明确分歧本质，避免人工评审陷入无意义的细节争论。

### 6. 人工仲裁（Human Arbitration）

- **操作主体**：产品负责人/评审委员会
- **飞书操作**：
  1. PM打开评审文档，查看全部证据、Agent意见、推演结果、冲突摘要，在「最终裁决」章节给出结论：通过/否决/延迟。
  2. 必须填写裁决理由、反对条件、重新评估条件三个字段，保证决策可追溯。
  3. 仅高分歧场景需要人工介入，低分歧方案系统可自动按规则收敛，人工仅做最终确认。
- **产出**：带完整依据的最终裁决，所有决策过程留痕。

### 7. 决策自动归档（Decision Memory）

- **操作主体**：飞书自动化
- **飞书操作**：
  1. 裁决完成后，系统自动提取文档中的全部核心信息（提案内容、各Agent评分、冲突点、裁决结果、边界条件），结构化写入Product Brain多维表格。
  2. 评审文档链接、相关证据链接自动关联至对应决策条目，形成完整决策档案。
  3. 自动触发Decision Mining Agent扫描新增记录，更新决策模式库。
- **产出**：一条可检索、可复用的决策记忆，反哺下一轮评审。

---

## 三、Product Brain 四层结构的飞书落地实现

四层结构全部基于飞书多维表格+智能伙伴实现，无需额外数据库或模型训练。

### 1. 第一层：Decision Records 决策案例库

- **载体**：飞书多维表格主表
- **核心字段**：提案ID、提案内容、品类、裁决结果、Context上下文、裁决理由、反对条件、重新评估条件、类似案例、决策模式、潜在盲区、关联文档链接、时间戳。
- **能力**：原生支持按裁决结果、品类、风险等级筛选，支持视图分组（如"延迟项目池""生物识别类决策"），新人可直接按场景检索历史决策。

### 2. 第二层：Decision Patterns 决策模式层

- **载体**：飞书多维表格分析表 + 仪表盘 + Decision Mining Agent
- **运行机制**：
  1. Decision Mining Agent每周自动扫描全量决策记录，提炼反复出现的决策规律（如"高技术感知≠高用户价值""软件创新ROI优于硬件堆料"），同步至「决策模式表」。
  2. 飞书仪表盘自动可视化展示：各品类否决率、Top 3失败原因、不同创新类型的通过率等数据。
  3. 所有决策模式会反向注入Agent的系统提示词，新提案评审时，Agent会先匹配对应模式，前置给出风险提示，实现"经验指导新决策"的闭环。

### 3. 第三层：Boundary Conditions 边界条件层

- **载体**：多维表格字段 + 飞书自动化触发
- **运行机制**：
  1. 每条决策记录都包含「反对条件」与「重新评估条件」两个结构化字段（如"BOM下降≥30%""GDPR相关法规更新"）。
  2. 配置飞书自动化：当企业规则库、供应链成本库、合规库的数据更新时，自动匹配符合重新评估条件的历史延迟项目，触发提醒给对应品类PM，建议重启评审。
  - 例：当掌静脉模组成本数据更新为下降32%时，系统自动找到BIO-001号记录，给智能锁产品线PM发飞书消息，提示"该项目满足重新评估条件，建议重启评审"。

### 4. 第四层：Challenger Memory 反路径依赖层

- **载体**：多维表格「潜在盲区」字段 + Anti-Bias Agent
- **运行机制**：
  1. 每次裁决时，必须同步填写「潜在盲区」（即"这个判断在什么情况下可能出错"）。
  2. 每次新提案检索历史决策时，Anti-Bias Agent会专门读取对应记录的潜在盲区，结合当前市场环境，判断历史结论是否存在路径依赖风险。
  3. 若风险匹配，自动在证据区标注「历史结论可能失效，建议重新评估」，避免经验固化阻碍创新。

---

## 四、典型场景实操示例：掌静脉提案完整走查

1. **录入**：PM在飞书表单提交"智能锁增加掌静脉识别"提案，自动生成提案ID BIO-001，系统自动创建评审文档。
2. **检索**：系统自动检索到3D人脸、UWB等同类硬件升级的历史否决记录，同时匹配GDPR生物信息合规规则、北美用户对复杂功能的差评数据，全部插入文档。
3. **评审**：成本、合规、用户替身Agent分别给出反对意见，市场、品牌Agent给出支持意见，各自在专属分区输出，互不影响。
4. **推演**：系统自动生成2026年落地、2027年Pro版落地、完全不做三个场景的成本、利润、竞品影响对比表。
5. **冲突提炼**：系统识别分歧本质为"营销价值vs用户感知价值"，属于高分歧，触发人工仲裁。
6. **仲裁**：PM给出"延迟至旗舰Pro版"的裁决，填写重新评估条件与潜在盲区。
7. **归档**：系统自动将全流程数据写入Product Brain，Decision Mining Agent同步更新"高技术感知≠高用户价值"模式的样本量。

---

## 五、分阶段落地节奏

### 第一阶段（1-2周）：最小可用版

搭好提案表单、评审文档模板、决策记录多维表格，Agent环节由人工替代，先跑通"标准化提案-人工评审-结构化归档"的流程，沉淀第一批决策数据。

### 第二阶段（3-4周）：AI辅助版

接入飞书智能伙伴，实现自动证据检索、Agent辅助评审、冲突自动提炼，人工做最终确认，大幅提升评审效率。

### 第三阶段（长期）：全自动进化版

上线反事实推演、决策模式自动挖掘、边界条件自动触发重评，实现全流程自动化运转，Product Brain随决策量增加持续进化。
